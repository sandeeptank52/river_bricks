/// Static, environment-independent URLs only. Anything that varies per
/// flavor (API endpoints, support/legal links) lives in [AppEnv]
/// (`app_env.dart`) — never here.
class AppUrls {
  AppUrls._();
}
