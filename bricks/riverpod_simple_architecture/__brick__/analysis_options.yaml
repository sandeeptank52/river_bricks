include: package:flutter_lints/flutter.yaml

# Riverpod-specific lints are currently disabled because riverpod_lint /
# custom_lint cannot yet be resolved alongside auto_route 11's generator
# (analyzer version conflict). Once the ecosystem realigns, add custom_lint +
# riverpod_lint to dev_dependencies and uncomment the plugin below.
# analyzer:
#   plugins:
#     - custom_lint

linter:
  rules:
    public_member_api_docs: false
